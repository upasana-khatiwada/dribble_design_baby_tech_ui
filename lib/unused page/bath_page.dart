import 'package:flutter/material.dart';

class BathPage extends StatelessWidget {
  const BathPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}